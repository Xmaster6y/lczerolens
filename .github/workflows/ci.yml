on:
  workflow_call:
    inputs:
      tests-type:
        required: true
        type: string

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Install just
      uses: extractions/setup-just@v3
      with:
        just-version: 1.41.0
    - name: Install uv
      uses: astral-sh/setup-uv@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        uv sync --locked --group dev --extra hub --extra datasets --extra viz --extra backends
    - name: Install notebook test dependencies
      if: inputs.tests-type == 'tests-slow'
      run: |
        uv sync --locked --group dev --group notebooks --extra hub --extra datasets --extra viz --extra backends
    - name: Run checks
      run: |
        just checks
    - name: Download and validate conformance fixtures
      if: inputs.tests-type == 'tests' || inputs.tests-type == 'tests-conformance'
      run: |
        just test-fixtures
    - name: Run tests
      run: |
        just ${{ inputs.tests-type }}
    - name: Upload test artifacts
      if: ${{ !cancelled() }}
      uses: actions/upload-artifact@v4
      with:
        name: test-artifacts-py${{ matrix.python-version }}-${{ inputs.tests-type }}
        path: |
          junit.xml
          coverage.xml
        if-no-files-found: ignore
    - name: Upload coverage reports to Codecov
      if: matrix.python-version == '3.11' && inputs.tests-type == 'tests'
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
    - name: Upload test results to Codecov
      if: matrix.python-version == '3.11' && inputs.tests-type == 'tests' && ${{ !cancelled() }}
      uses: codecov/test-results-action@v1
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        fail_ci_if_error: true

  installation:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: base
            sync-args: --no-default-groups
            import: import lczerolens
          - name: hub
            sync-args: --no-default-groups --extra hub
            import: import lczerolens, huggingface_hub
          - name: datasets
            sync-args: --no-default-groups --extra datasets
            import: import lczerolens, datasets, sklearn
          - name: viz
            sync-args: --no-default-groups --extra viz
            import: import lczerolens, graphviz, matplotlib
          - name: backends
            sync-args: --no-default-groups --extra backends
            import: import lczerolens; from lczero.backends import Backend
          - name: docs
            sync-args: --no-default-groups --group docs
            import: import lczerolens, sphinx
    name: Installation (${{ matrix.name }})
    steps:
    - uses: actions/checkout@v4
    - name: Install uv
      uses: astral-sh/setup-uv@v5
      with:
        python-version: "3.11"
    - name: Install selected package dependencies
      run: uv sync --locked ${{ matrix.sync-args }}
    - name: Check imports
      run: uv run --no-sync python -c "${{ matrix.import }}"
